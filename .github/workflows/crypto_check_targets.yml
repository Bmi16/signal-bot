name: Check Crypto Targets

on:
  schedule:
      - cron: "*/15 * * * *"    
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: state-crypto
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check crypto targets
        env:
          CRYPTO_BOT_TOKEN: ${{ secrets.CRYPTO_BOT_TOKEN }}
          CRYPTO_CHANNEL_ID: ${{ secrets.CRYPTO_CHANNEL_ID }}
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
        run: python crypto_check_targets.py

      - name: Commit updated state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data
          git diff --cached --quiet || git commit -m "Update crypto signals state [skip ci]"
          git pull --rebase
          git push
